"""fftkit — one FFT API over many backends.

The public API is assembled in Phase 2 of the extraction; this module currently
carries only the version so the package is importable and installable.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
